#include<stdio.h>

#include<stdlib.h>
#include<string.h>
#include<glib.h>

int main()

{

GSList *list = NULL;

char *f_read = NULL;

char **f_lines = NULL;

char **f_line_parts = NULL;

g_file_get_contents("./test",&f_read,NULL,NULL);

f_lines = g_strsplit_set(f_read,"\n",-1);

int no_of_lines = g_strv_length(f_lines);

int i;

for(i=0;i<no_of_lines;i++)

{

	f_line_parts = g_strsplit_set(f_lines[i]," \t,;",-1);

	int size = g_strv_length(f_line_parts);

	int j;

	for(j=0;j<size;j++)

	{

		printf("%s \t",f_line_parts[j]);
		printf("%ld\n", strlen(f_line_parts[j]));

	}

}

return(0);

}








